{
  "name": "hello-copilot",
  "version": "1.0.0",
  "type": "module",
  "description": "Level 1: Your First SDK Session — setup, send, receive, clean up",
  "scripts": {
    "hello": "npx tsx hello.ts",
    "models": "npx tsx try-models.ts",
    "debug": "npx tsx debug-logging.ts",
    "errors": "npx tsx error-handling.ts",
    "config": "npx tsx explore-config.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@github/copilot-sdk": "^0.1.32",
    "tsx": "^4.21.0"
  }
}
