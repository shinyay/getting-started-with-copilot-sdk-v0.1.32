# syntax=docker/dockerfile:1

# -----------------------------------------------------------
# Dev Container for Copilot SDK Workshop
#
# Base: Microsoft's devcontainer Node.js 22 image
# This image includes Node.js 22, npm, git, and common tools.
# Additional tools (GitHub CLI) are added via devcontainer
# features in devcontainer.json, not here.
# -----------------------------------------------------------

FROM mcr.microsoft.com/devcontainers/javascript-node:22

# Set workspace directory
WORKDIR /workspaces/getting-started-with-copilot-sdk
